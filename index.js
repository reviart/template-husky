async function main() {
  console.log('This repo is template to use husky to validate lint code (for example), and i set husky into this repo not for single device, while other devs use this repo, they can use automatically without any setup.');
  process.exit(0);
}

main();
